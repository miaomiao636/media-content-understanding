# 安全策略

## 支持范围

请只报告当前主分支和最新正式版本中的安全问题。

## 报告内容

公开仓库启用安全公告后，请通过 GitHub Private Vulnerability Reporting 报告。报告中包括受影响版本、复现步骤、影响范围和建议修复方向。不要在公开 Issue 中提交真实 API Key、Cookie、签名 URL、个人视频或其他敏感信息。

## 安全边界

- 只接受抖音和哔哩哔哩的 `http(s)` 链接。
- 不支持绕过验证码、付费、私密、DRM 或地域限制。
- 不导入日常 Chrome 或 Ego 的 Cookie；持久 Playwright 登录档案只能由用户明确配置。
- 浏览器媒体下载只能携带 Playwright 判定适用于目标 URL 的 Cookie；跨域重定向移除 Cookie 与认证头。
- 远程图片和视频的初始 URL、DNS 结果及每次重定向都必须通过公开网络地址检查，并使用已验证 IP 建立连接。
- 密钥来自环境变量或系统密钥管理器，不进入仓库、配置模板和日志。
- 缓存清理仅操作带本 Skill 管理标记的任务目录。
- 媒体大小、抽帧数、模型调用数和超时必须有限制。
