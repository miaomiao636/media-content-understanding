# 技术决策记录

> 本文件记录能够从当前代码、文档和 Git 历史确认的长期决策。日期均以首次公开提交和当前版本记录为依据。

## 2026-08-29：将视频理解与知识库入库拆分

### 决策

本仓库只实现链接获取、字幕/ASR、视觉理解、内容提炼和证据输出，不实现 Obsidian 入库、分类或知识沉淀工作流。

### 原因

- 视频理解可以作为多个知识管理流程复用的基础能力。
- 避免把 Obsidian 路径、目录规则和用户知识库结构绑定到公共 Skill。

### 影响

- `SKILL.md` 明确排除 Obsidian ingestion。
- 后续完整知识库流程应作为独立 Skill 或编排层消费 `media-analysis-package`。

## 2026-08-29：采用 Agent Skill + 本地 CLI，而非常驻服务

### 决策

`SKILL.md` 负责触发条件和 Agent 工作流，Python CLI 负责可重复的本地执行；不建立 Web 后端、MCP Server 或数据库。

### 原因

- 便于通过 GitHub 直接分发到不同设备和 Agent。
- 视频获取、FFmpeg、浏览器和本地缓存天然适合设备侧执行。
- 减少部署和运维依赖。

### 影响

- 目标 Agent 必须支持 Skill 且允许执行本地 Python/系统命令。
- 不允许本地命令的 Agent 只能把本仓库当流程说明使用。

## 2026-08-29：来源适配器使用确定的回退顺序

### 决策

来源获取顺序为 `yt-dlp` 主路径、Playwright 隔离浏览器回退。只接受抖音和哔哩哔哩 `http(s)` 域名，短链跳转后再次验证最终域名。

### 原因

- `yt-dlp` 提供结构化元数据、字幕和媒体获取。
- 平台风控或页面差异导致主路径失败时，真实浏览器网络响应可作为补充。
- URL 白名单和二次验证降低 SSRF 与非预期下载风险。

### 影响

- 平台差异封装在 `scripts/source_adapter.py`。
- B站下载使用 `--no-playlist`，避免意外批量处理合集。
- Ego Browser 当前是宿主可替代能力和预检项，不是 CLI 内置适配器。

## 2026-08-29：浏览器登录态必须由用户明确授权

### 决策

默认不读取个人浏览器 Cookie，也不跨任务保存 Playwright 状态。用户主动设置 `acquisition.cookie_browser` 时，才向 `yt-dlp` 传递 `--cookies-from-browser`；用户主动设置 `acquisition.browser_profile_dir` 时，Playwright 才在该 Skill 的独立目录持久化登录状态。

### 原因

- 避免未经授权导出登录态。
- 保持公共 Skill 的隐私和安全边界。
- 解决一次性 Playwright 上下文导致用户每条抖音任务都要重新登录的问题，同时不污染日常浏览器档案。

### 影响

- 需要登录、验证码、私密或权限控制时可以失败并报告，但不得绕过控制。
- Playwright 未配置档案时使用一次性独立上下文；配置档案时使用专用持久上下文。抖音默认可见模式，以降低只得到数秒预览的概率。
- 专用档案必须与缓存、输出、用户主目录和根目录分离；重置需要显式确认，并发占用会返回专用错误。
- 适配器只采用空目录或带有效 Skill 管理标记的目录；清除命令还会拒绝项目目录，避免配置错误导致删除普通文件。
- 2026-08-29 本机连续两次真实抖音获取验证了会话复用；平台主动使会话过期时仍可能要求重新登录。

## 2026-08-29：文字获取按可靠性逐级降级

### 决策

顺序为平台字幕、本地 `faster-whisper`、支持原生视频的外部视觉模型分段转写；全部不可用时保留部分结果。

### 原因

- 平台字幕成本低且通常具有时间轴。
- 本地 ASR 不需要把完整音频提交到第三方。
- 原生视频模型可以在前两者不可用时同时理解声音与画面。

### 影响

- `faster-whisper` 是可选依赖。
- 原生视频转写按 180 秒分段，并与最终摘要、重试、故障切换和复核共享 `max_visual_calls`；转写至少为最终摘要保留一次 provider 尝试。

## 2026-08-29：外部视觉模型优先，并按 provider 故障切换

### 决策

只选择能力匹配的已启用 provider，按 `priority` 数值升序调用；配置、认证类错误直接切换，网络、超时、429、5xx 和无效响应有限重试后切换。外部链耗尽后由宿主 Agent 按 Skill 说明决定是否接管。

### 原因

- 允许用户独立配置主模型和备用模型。
- 单一视觉服务商故障不应立即终止整个视频理解任务。
- 用户需要看到每个模型的错误类型和可执行建议。

### 影响

- 示例顺序为千问 `qwen3.5-omni-plus`，再到 Xiaomi MiMo `mimo-v2.5`。
- 报告会保存已尝试 provider、脱敏错误、建议和是否允许宿主回退。
- 主结果末尾的结构化 `MCU_CONFIDENCE` 标记决定是否复核；只有明确的 `low` 才调用下一 provider，自由文本中的不确定措辞不作为触发条件。

## 2026-08-30：视觉限制采用共享保守预算和双层上传约束

### 决策

一次 `mcu analyze` 的所有外部视觉尝试共享 `vision.max_visual_calls`。每次进入 provider 尝试即扣减一次；子进程缺少可信用量报告时耗尽剩余预算。单次请求的本地 Base64 媒体合计受 `vision.max_upload_mb` 限制，单个媒体同时受 provider 专属限制。

### 原因

- 重试和故障切换也会产生调用与潜在成本，不能只按视频分段数计数。
- 多张图片即使各自满足 provider 限制，合计请求仍可能过大。
- 自由文本置信度猜测不可稳定测试，结构化末尾标记可验证、可审计。

### 影响

- `vision_router.py` 报告 `api_calls_limit`、`api_calls_used`、`budget_exhausted` 和复核状态。
- `mcu.py` 在原生视频转写和最终视觉摘要之间传递同一预算，并把显式配置路径传给子流程。
- CLI 的 `--max-api-calls` 只能缩小、不能扩大配置中的全局上限。

## 2026-08-29：密钥不进入配置和仓库

### 决策

API Key 从环境变量或系统凭据存储解析；环境变量优先。macOS 使用 Security.framework/Keychain，其他系统使用 Python Keyring。

### 原因

- 公共仓库和输出包不能携带用户密钥。
- 允许本地持久化，同时支持 CI、服务器和 Secret Manager。

### 影响

- `assets/config.example.json` 只包含非敏感配置和密钥引用名。
- 错误、日志和 Context 必须脱敏。

## 2026-08-29：输出采用文件包契约并保持可审计

### 决策

使用 `media-analysis-package` 1.0 保存来源、摘要、逐字稿、错误和媒体证据。统一 CLI 先写 `partial`，宿主 Agent 复核后才能标记 `completed`。

### 原因

- 文件包便于跨 Agent、跨设备和后续知识库流程读取。
- 证据路径、时间点和限制比只有摘要更容易验证。
- 视觉证据选择需要 Agent 判断，当前不由确定性 CLI 假装自动完成。

### 影响

- `scripts/package_tool.py` 提供结构校验。
- `completed` 包必须有非空摘要，媒体路径必须在包内且真实存在。
- 下游流程必须区分 `partial` 与 `completed`。
- 当前候选工作区新增显式 `mcu finalize`；只有结构、必要证据和严重事实冲突检查均通过时才原子写入 `completed`。

## 2026-08-29：缓存删除采用标记与显式确认

### 决策

清理器只操作配置的安全 `temp_root`；根目录和任务目录必须分别带管理标记。手工命令默认只预览，只有 `--apply` 才删除；统一 CLI 创建新任务前会按已确认的保留策略自动应用清理。

### 原因

- 视频缓存体积大，但自动递归删除存在高风险。
- 标记机制可以缩小误删范围。

### 影响

- 输出目录不能等于缓存目录。
- 任务记录 `running/completed/failed` 状态；失败任务使用独立保留时间，其他任务使用通用 TTL。
- 容量超限时优先清理最旧的非运行任务，近期运行任务不会因容量压力被删除。
- `analyze` 仅在输出包校验通过后清理当前任务；`acquire` 的结果会保留在受控缓存中。

## 2026-08-29：运行参数采用 CLI、用户配置、内置默认值三级优先级

### 决策

只有显式提供的 CLI 参数才覆盖用户配置；未提供时读取 `config.json`，配置仍缺失时使用程序默认值。

### 原因

- 避免 argparse 固定默认值静默遮蔽用户配置。
- 让同一份配置在不同 Agent 和设备上产生可预测行为。

### 影响

- ASR 模式、模型、语言和故事板帧数已经按该规则解析。
- 下载上限由统一配置传入所有来源适配器。
- 配置加载时验证关键数值，并使用深复制避免污染全局默认配置。

## 2026-08-29：跨平台输出统一使用 UTF-8

### 决策

Python 输出流和子进程输出显式采用 UTF-8，并在 CI 中覆盖三种操作系统和多个 Python 版本。

### 原因

- Windows 默认控制台编码曾导致中文错误报告异常。

### 影响

- 命令行脚本统一调用 `configure_utf8_stdio()`。
- CI 当前覆盖 Python 3.9、3.11、3.13。

## 2026-08-30：最终证据采用规则规划、实际生成和失败降级

### 决策

关键截图和动态短片由字幕视觉触发词与场景变化共同规划，设置数量上限并去重。只有实际生成成功的媒体进入 `manifest.media`；短片失败时降级为截图，并把限制和错误写入包。

### 原因

- “这个效果”“像这样”等内容不能只依赖文字摘要。
- 把所有故事板长期保存会增加噪声、体积和视觉调用成本。
- 动态效果提取失败不应使整个理解任务丢失可复核画面。

### 影响

- `scripts/evidence_selector.py` 负责证据规划。
- `scripts/media_tools.py` 在 FFmpeg 解码阶段缩放故事板，减少高分辨率源的无效开销。
- 自动选择仍属于候选证据，最终结论继续受 `finalize` 门禁约束。

## 2026-08-30：抖音非视频来源与视频来源共用安全边界

### 决策

抖音图文、长文本和混合内容在独立适配器中解析，区分作者正文、标题、作者、发布时间和原序图片。图片下载必须执行协议、域名、私网、数量和大小限制，并把 TCP 连接锁定到已验证公网 IP；登录或挑战明确失败，不尝试绕过。

### 原因

- 图文内容不能通过伪造视频、转写或时间戳表达。
- DNS 预检后再次解析会留下重绑定/TOCTOU 私网访问风险。
- 平台挑战属于访问控制边界，不是应由公共 Skill 绕过的故障。

### 影响

- `scripts/douyin_content_adapter.py` 输出 `long_text/gallery/mixed` 来源数据。
- 真实成功收据只证明记录时刻的公开样本可用，不构成平台永久可用承诺。
- 统一非视频分析包仍由 FINAL-005 接入 `mcu analyze`。

## 2026-08-31：执行与最终审核分离

### 决策

FINAL-005～007 的后续候选制作交给 Claude Code；Codex 主 Agent不直接实现这些业务成果，只负责范围控制、独立验收门禁、最终代码审核、总体验证、Git 提交、推送和发布收尾。

### 原因

- 用户明确要求由其他 Agent 接手后续制作。
- 执行者不能验收自己的成果，最终发布需要独立审查。
- `.agent-workflow/` 已具备持久状态和恢复协议，可在不依赖聊天历史的情况下交接。

### 影响

- Claude Code 以 `CLAUDE_CODE_TASKS.md`、当前代码、Context、任务 JSON 和报告为事实来源；不直接编辑 Codex 的 `.agent-workflow` JSON。
- 稳定版 `v0.3.0` 仍需用户确认；执行 Agent 不得擅自发布。
- `AGENTS.md`、密钥、Cookie、浏览器档案和个人配置不进入提交或发布包。

## 2026-08-31：图文完成状态必须包含图片层校订

### 决策

`gallery` 和 `mixed` 包的 `image-analysis.md` 仍含“尚未校订”等占位内容时，`mcu finalize` 必须保持 `partial`。图片存在和摘要改写本身不能证明视觉理解已经完成。

### 原因

- 原实现只检查媒体文件存在，允许未校订 OCR/画面细节的包进入 `completed`。
- 用户要求无法用文字表达的画面必须被实际理解，而不是只保存图片。

### 影响

- 新增 `IMAGE_ANALYSIS_REVIEW_REQUIRED` / `IMAGE_ANALYSIS_MISSING` 阻断项。
- 外部视觉结果必须覆盖每个图片序号；宿主接管时需实质改写图片分析层。
- 最终审计同时强化作品 ID 强匹配、获取错误脱敏和 Bundle 敏感路径检查。

## 2026-08-31：浏览器验证与诊断安全采用共享边界

### 决策

视频与抖音图文适配器共用 `scripts/browser_verification.py`，同时检查 URL、标题和可见正文中的登录、验证码与滑块信号；只有检测到用户动作要求时才限时等待，超时明确返回挑战错误。持久化或展示错误前统一使用共享脱敏器，事实审计不把 ISO 日期、域名、文件名和元数据字段当作数值范围或软件名。

### 原因

- 两份浏览器等待实现已经产生登录检测差异，且只看 URL/标题会漏掉 DOM 弹窗。
- 异常字符串常使用 Python 字典或普通 `key=value`，仅覆盖 HTTP 头不足以防止 Cookie/Token 泄漏。
- 技术 token 的宽松正则会把日期、文件和内部字段变成虚假严重冲突。

### 影响

- 新增浏览器正常页、正文登录/挑战、恢复和超时回归测试。
- 脱敏覆盖请求头、带引号字典和普通键值中的常见凭据。
- 通用技术 token 必须包含数字；无版本软件继续由维护的已知软件集合识别。

## 2026-09-01：浏览器会话、远程媒体和事实审计按最小权限处理

- Ego Browser 继续作为宿主可选能力，不成为公共 CLI 的强制依赖；内置浏览器回退仍使用 Playwright 专用档案，以保证没有 Ego 的 Agent 和设备可运行。
- Playwright 每次可以打开新窗口，但固定 `browser_profile_dir` 代表复用同一专用会话。不得把日常 Chrome、Ego 任务空间和 Playwright 档案视为同一登录来源。
- 捕获媒体时逐 URL 调用浏览器 Cookie 过滤；不再导出整个上下文 Cookie 集合。跨域重定向移除 Cookie 与认证头。
- 图片和视频下载共用公网 IP 验证、DNS 锁定和逐跳重定向检查，拒绝回环、私网、链路本地和其他非公开地址。
- 事实审计只在同对象、不同来源之间判定矛盾；同一来源中与摘要精确匹配并列出现的其他正确声明不互相冲突。版本号优先绑定最近的软件对象。
- 视觉摘要路由启动失败或整体超时时保守耗尽未知预算、记录 `VISION_ROUTER_FAILED` 并保留 `partial` 结果。
- 发布前必须验证现有 Skill ZIP 与当前源码逐文件一致；仅校验 ZIP 自身哈希不足以证明它代表最终工作树。
- ZIP 条目与 manifest 键必须使用 POSIX `/` 分隔符，避免 Windows `Path` 的 `\` 与 ZIP 标准路径不一致。干净 CI Runner 的锁文件检查不使用 `--offline`，因为 setup-uv 需先安装矩阵指定的 Python。
- `v0.3.0-rc.1` 只作为跨设备、跨 Agent 验收用 Pre-release；即使本地门禁和远程 18 个任务全部通过，稳定版 `v0.3.0` 仍须等待用户验收确认。

## 2026-09-01：Markdown 为主文档，HTML 为可移植阅读层

- `summary.md` 继续作为人和 Agent 可编辑的唯一摘要源，`summary.html` 从 Markdown 和 `manifest.media` 确定性生成，不建立第二份可独立编辑的内容。
- `analyze` 生成初版 HTML，`finalize` 依据最新 Markdown 刷新；用户也可显式执行 `render-html`。
- HTML 不含 JavaScript 或在线 CDN，禁用原始 Markdown HTML，并只显示理解包内真实存在的图片和短片，防止包外本机文件和远程跟踪资源被意外加载。
- 为保持旧包可验证，`content.summary_html_file` 对旧包兼容可选；新包固定声明并生成 `summary.html`。
